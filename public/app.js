const input=document.querySelector('#input'), chat=document.querySelector('#chat'), send=document.querySelector('#send'), voice=document.querySelector('#voice'), connect=document.querySelector('#connect'), status=document.querySelector('#status'), dot=document.querySelector('#dot');
function say(text,who='bot',speak=false){const d=document.createElement('div');d.className=`bubble ${who}`;d.textContent=text;chat.appendChild(d);chat.scrollTop=chat.scrollHeight;if(speak&&'speechSynthesis' in window){speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance(text));}}
async function refresh(){try{const r=await fetch('/api/status'),j=await r.json();status.textContent=j.connected?'Google Calendar connected':'Connect Google Calendar to begin';dot.className=j.connected?'ok':''}catch{status.textContent='Assistant unavailable'}}
async function run(){const command=input.value.trim();if(!command)return;say(command,'user');input.value='';send.disabled=true;voice.disabled=true;try{const r=await fetch('/api/command',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({command})}),j=await r.json();if(!r.ok)throw new Error(j.error);let text=j.message||'Done.';if(j.events?.length)text+='\n'+j.events.map(e=>`• ${e.summary} — ${e.start?.dateTime||e.start?.date}`).join('\n');say(text,'bot',true)}catch(e){say(`I couldn't do that: ${e.message}`,'bot',true)}finally{send.disabled=false;voice.disabled=false;refresh()}}
send.onclick=run;input.onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();run()}};connect.onclick=()=>location.href='/auth/google';
const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(Recognition){const rec=new Recognition();rec.lang='en-US';rec.interimResults=true;rec.continuous=false;voice.onclick=()=>{rec.start();voice.textContent='Listening…'};rec.onresult=e=>{input.value=Array.from(e.results).map(x=>x[0].transcript).join('');if(e.results[e.results.length-1].isFinal)run()};rec.onend=()=>voice.textContent='🎙️ Speak';rec.onerror=()=>{voice.textContent='🎙️ Speak';say('I could not hear that clearly. Please try again.','bot',true)}}else{voice.disabled=true;voice.textContent='Voice unavailable'}refresh();
